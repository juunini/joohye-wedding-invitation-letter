import { Meta, Canvas, Story } from '@storybook/addon-docs';

import SaveTheDate from './SaveTheDate';
import FadeInArticle from './FadeInArticle';

<Meta title="Components" />

<Canvas>
  <Story
    name="SaveTheDate"
    argTypes={{
      duration: { control: 'number', defaultValue: 10 },
      radius: { control: 'number', defaultValue: 80 },
      fontSize: { control: 'number', defaultValue: 16 },
      letterSpacing: { control: 'number', defaultValue: 10.5 },
    }}
  >
    {
      ({
        duration,
        radius,
        fontSize,
        letterSpacing,
      }) => (
        <SaveTheDate
          duration={duration}
          radius={radius}
          fontSize={`${fontSize}px`}
          letterSpacing={`${letterSpacing}px`}
        />
      )
    }
  </Story>
</Canvas>

<Canvas>
  <Story
    name="FadeInArticle"
    argTypes={{
      duration: { control: 'number', defaultValue: 1.5 },
      from: { control: 'text', defaultValue: '-20%' },
      to: { control: 'text', defaultValue: '0' },
      direction: {
        control: { type: 'select'},
         options: ['top', 'bottom', 'left', 'right'],
        defaultValue: 'left',
      },
    }}
  >
    {
      ({ duration, direction, from, to }) => (
        <FadeInArticle
          direction={direction}
          duration={duration}
          from={from}
          to={to}
        >
          aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          <br />
          aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          <br />
          aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          <br />
          aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          <br />
          aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          <br />
        </FadeInArticle>
      )
    }
  </Story>
</Canvas>
