import { Meta, Canvas, Story } from '@storybook/addon-docs';

import Section1 from '.';
import Title from './Title';
import Name from './Name';
import Date from './Date';
import Address from './Address';
//
const DefaultValues = {
  'title.lineHeight': { control: 'number', defaultValue: 0.9 },
  'title.fontSize': { control: 'number', defaultValue: 90 },
  'title.fontWeight': { control: 'number', defaultValue: 500 },
  'name.fontSize': { control: 'number', defaultValue: 20 },
  'name.fontWeight': { control: 'number', defaultValue: 500 },
  'date.letterSpacing': { control: 'number', defaultValue: 1.4 },
  'date.fontSize': { control: 'number', defaultValue: 28 },
  'date.fontWeight': { control: 'number', defaultValue: 500 },
  'address.letterSpacing': { control: 'number', defaultValue: 1.4 },
  'address.fontSize': { control: 'number', defaultValue: 28 },
  'address.fontWeight': { control: 'number', defaultValue: 300 },
}
const renderSection1 = (props) => (
  <Section1
    title={{
      lineHeight: props['title.lineHeight'],
      fontSize: props['title.fontSize'],
      fontWeight: props['title.fontWeight'],
    }}
    name={{
      fontSize: props['name.fontSize'],
      fontWeight: props['name.fontWeight'],
    }}
    date={{
      letterSpacing: props['date.letterSpacing'],
      fontSize: props['date.fontSize'],
      fontWeight: props['date.fontWeight'],
    }}
    address={{
      letterSpacing: props['address.letterSpacing'],
      fontSize: props['address.fontSize'],
      fontWeight: props['address.fontWeight'],
    }}
  />
);

<Meta title="Components/Sections/Section1" />

<Canvas>
  <Story
    name="Section1"
    argTypes={DefaultValues}
  >
    {renderSection1}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Section1 (1200px)"
    argTypes={DefaultValues}
  >
    {
      (props) => (
        <div style={{ width: '1200px', margin: 'auto', border: '1px solid black' }}>
          {renderSection1(props)}
        </div>
      )
    }
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Section1 (375 x 667)"
    argTypes={DefaultValues}
  >
    {
      (props) => (
        <div style={{ overflow: 'hidden', width: '375px', height: '667px', margin: 'auto', border: '1px solid black' }}>
          {renderSection1(props)}
        </div>
      )
    }
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Title"
    argTypes={{
      lineHeight: { control: 'number', defaultValue: 0.9 },
      fontSize: { control: 'number', defaultValue: 90 },
      fontWeight: { control: 'number', defaultValue: 500 },
    }}
  >
    {
      ({
        lineHeight,
        fontSize,
        fontWeight,
      }) => (
        <Title
          fontWeight={fontWeight}
          fontSize={`${fontSize}px`}
          lineHeight={lineHeight}
        >
          We
          <br />
          are
          <br />
          getting
          <br />
          married
        </Title>
      )
    }
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Name"
    argTypes={{
      fontSize: { control: 'number', defaultValue: 20 },
      fontWeight: { control: 'number', defaultValue: 500 },
    }}
  >
    {
      ({
        fontSize,
        fontWeight,
      }) => (
        <div style={{ position: 'relative', maxWidth: '720px', height: '600px' }}>
          <Name
            fontWeight={fontWeight}
            fontSize={`${fontSize}px`}
            rotate={-90}
            left="calc(-100px + 50 / 720px * 100%)"
          >
            KIM CHEOL SOO
          </Name>
          <Name
            fontWeight={fontWeight}
            fontSize={`${fontSize}px`}
            rotate={-90}
            right="-50px"
          >
            YANG JOO HYE
          </Name>
        </div>
      )
    }
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Date"
    argTypes={{
      letterSpacing: { control: 'number', defaultValue: 1.4 },
      fontSize: { control: 'number', defaultValue: 28 },
      fontWeight: { control: 'number', defaultValue: 500 },
    }}
  >
    {
      ({
        letterSpacing,
        fontSize,
        fontWeight,
      }) => (
        <Date
          fontWeight={fontWeight}
          fontSize={`${fontSize}px`}
          letterSpacing={`${letterSpacing}px`}
        >
          2022.10.03 MON 14:00
        </Date>
      )
    }
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Address"
    argTypes={{
      letterSpacing: { control: 'number', defaultValue: 1.4 },
      fontSize: { control: 'number', defaultValue: 28 },
      fontWeight: { control: 'number', defaultValue: 300 },
    }}
  >
    {
      ({
        letterSpacing,
        fontSize,
        fontWeight,
      }) => (
        <Address
          fontWeight={fontWeight}
          fontSize={`${fontSize}px`}
          letterSpacing={`${letterSpacing}px`}
        >
          로프트가든344
        </Address>
      )
    }
  </Story>
</Canvas>
